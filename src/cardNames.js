import cardImages from "./cardImages";

const cardNames = Object.keys(cardImages);

// ["Charmander", "Squirtle", ...]
/* 
To select name, 

cardNames[0]
or
cardNames[cardNames.indexOf("Charmander")];



*/

export default cardNames;
